{
  "kind": "collectionType",
  "collectionName": "quizzes",
  "info": {
    "name": "Quiz"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "unique": true
    },
    "questions": {
      "collection": "question",
      "via": "quiz"
    },
    "results": {
      "collection": "result",
      "via": "quiz"
    }
  }
}
